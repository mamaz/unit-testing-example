import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class GasolineCell extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

GasolineCell.propTypes = propTypes;
export default GasolineCell;
